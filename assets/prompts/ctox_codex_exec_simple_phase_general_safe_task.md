Main meta-skill: `general-safe-task`

Use inside CTOX:

- Use `Focus`, `Verified evidence`, `Anchors`, and `Workflow state` to keep the task bounded.
- This is a fallback method, not a replacement for the CTOX core prompt.
- Re-classify to a more specific phase as soon as the task shape is clearer.

- Use when no more specific main phase fits well enough.
- Start with one small inspect step.
- Then choose one small local action and one small check.
- Keep scope narrow until a more specific phase becomes clear.
- Re-classify as soon as the task shape is clearer.
