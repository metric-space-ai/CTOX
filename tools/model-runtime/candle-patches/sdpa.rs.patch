--- /Users/michaelwelsch/.cargo/git/checkouts/candle-5b4d092929d18d36/c3bb5bf/candle-metal-kernels/src/kernels/sdpa.rs	2026-04-19 00:00:24
+++ /Users/michaelwelsch/candle-fork-ctox/candle-metal-kernels/src/kernels/sdpa.rs	2026-04-20 19:36:55
@@ -227,10 +227,8 @@
         height: WM,
         depth: WN,
     };
-    encoder.use_resource(q_buffer, MTLResourceUsage::Read);
-    encoder.use_resource(k_buffer, MTLResourceUsage::Read);
-    encoder.use_resource(v_buffer, MTLResourceUsage::Read);
-    encoder.use_resource(output, MTLResourceUsage::Write);
+    // useResource removed — direct setBuffer bindings already track these.
+    let _ = (q_buffer, k_buffer, v_buffer, output);
     encoder.dispatch_thread_groups(grid_dims, group_dims);
 
     Ok(())
@@ -338,10 +336,8 @@
         height: 1,
         depth: 1,
     };
-    encoder.use_resource(q_buffer, MTLResourceUsage::Read);
-    encoder.use_resource(k_buffer, MTLResourceUsage::Read);
-    encoder.use_resource(v_buffer, MTLResourceUsage::Read);
-    encoder.use_resource(output, MTLResourceUsage::Write);
+    // useResource removed — direct setBuffer bindings already track these.
+    let _ = (q_buffer, k_buffer, v_buffer, output);
     encoder.dispatch_thread_groups(grid_dims, group_dims);
     Ok(())
 }
