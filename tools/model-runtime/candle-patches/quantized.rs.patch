--- /Users/michaelwelsch/.cargo/git/checkouts/candle-5b4d092929d18d36/c3bb5bf/candle-metal-kernels/src/kernels/quantized.rs	2026-04-19 00:00:24
+++ /Users/michaelwelsch/candle-fork-ctox/candle-metal-kernels/src/kernels/quantized.rs	2026-04-20 19:36:27
@@ -167,9 +167,12 @@
             r3
         )
     );
-    encoder.use_resource(lhs, MTLResourceUsage::Read);
-    encoder.use_resource(rhs, MTLResourceUsage::Read);
-    encoder.use_resource(dst, MTLResourceUsage::Write);
+    // useResource removed: resources are already tracked by Metal via
+    // setBuffer bindings; calling useResource on top adds driver-side
+    // overhead without benefit for direct-bound matmul. This was 3
+    // redundant syscalls per Q4K matmul dispatch (~9300 per E4B decode
+    // token). Skip them.
+    let _ = (lhs, rhs, dst);
 
     encoder.dispatch_thread_groups(thread_groups_count, threads_per_threadgroup);
     Ok(())
@@ -273,9 +276,8 @@
             r3
         )
     );
-    encoder.use_resource(src0, MTLResourceUsage::Read);
-    encoder.use_resource(src1, MTLResourceUsage::Read);
-    encoder.use_resource(dst, MTLResourceUsage::Write);
+    // useResource removed: redundant with setBuffer direct bindings.
+    let _ = (src0, src1, dst);
 
     encoder.set_threadgroup_memory_length(0, 8192);
 
