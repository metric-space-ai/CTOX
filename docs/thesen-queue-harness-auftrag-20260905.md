Auftrag: anhaltende Probleme zwischen Queue, Harness und Business-OS-Anbindung beheben. Befundlage vom 04.09.2026 auf der Kundeninstanz thesen.ctox.dev (Release branch-main-20260904T161717Z). Vollständiges Protokoll mit Messwerten: docs/thesen-outbound-fehlerprotokoll-20260904.md (liegt auf origin/main als 391508cd8, Abschnitte 8–10). Lies es zuerst.

Arbeite in diesem Checkout (/Users/michaelwelsch/Documents/ctox) auf main. ACHTUNG: dieser Checkout ist gegenüber origin/main abgedriftet (miss zuerst: git fetch origin && git rev-list --left-right --count origin/main...HEAD). Die Kundeninstanz baut ausschließlich aus GitHub origin/main. Landen deshalb so: Änderungen hier entwickeln und testen, dann in einen sauberen Klon von origin/main (git clone --shared --single-branch --branch main ... unter ~/.local/state/workjet-launchpads/) cherry-picken, dort cargo check, dann pushen. Kein Push aus dem abgedrifteten Checkout. Das Upgrade auf der Kundeninstanz (ctox upgrade --dev) führe ICH aus, nicht du – melde nur, wann origin/main bereit ist.

Fünf Befunde, jeweils mit gemessener Evidenz. Reihenfolge = Priorität.

1) Kein Verfall abgelaufener Leases (schwerster Befund). Sieben Queue-Aufgaben standen seit Wochen auf `running`/geleast, ihre Arbeiter waren längst tot. Sie belegten alle Arbeitsplätze; von acht "laufenden" Aufgaben war eine echt. Nach manuellem Stornieren der sieben wurde die nächste wartende Aufgabe (BÜFA) sofort geleast. Erwartet: Leases haben Heartbeat/TTL; ohne Lebenszeichen fällt die Aufgabe nach begrenzter Zeit zurück auf `queued` (oder `failed` mit Grund), auch nach Daemon-Neustart (Reclaim beim Start). Abnahme: Test, der eine Lease ohne Heartbeat altern lässt und den Rückfall belegt; Test für Reclaim beim Start.

2) Selbstverstärkende Wiederholung von "repair scrape target <ziel>". Drei Wellen an einem Tag: 39, 17 und 29 Dubletten (11× maps-google-com, 10× justizonline-gv-at, 8× handelsregister-de, dazu bundesanzeiger-de, zefix-ch, evi-gv-at). Die App-seitige Entdopplung (configuration_digest im Policy-Datensatz) hält – die Dubletten entstehen serverseitig: jeder gescheiterte Reparaturlauf stellt offenbar den nächsten ein. Erwartet: Einreihung je Ziel idempotent (Entdopplungsschlüssel = Zieltyp+Ziel, solange eine offene Aufgabe existiert) und begrenzte Wiederholungen mit Backoff, danach `failed` mit Grund. Abnahme: Test, der dieselbe Reparatur dreimal einreiht und genau eine offene Aufgabe zählt; Test für das Wiederholungslimit.

3) Warteschlange nutzt ihre Kapazität nicht. Nach dem Stornieren der Leichen: dauerhaft 1 geleast bei 23 wartend, obwohl zuvor 8 gleichzeitig `running` waren. Ich habe das gemessen, nicht diagnostiziert. Finde heraus, was serialisiert (Begrenzung je Thread-Key/Modul? Leasing-Schleife vergibt je Durchlauf nur eine?). Wenn es eine bewusste Grenze ist: benennen und konfigurierbar über typed config/SQLite-Runtime-Store machen (kein Env-Toggle). Wenn Bug: fixen. Abnahme: Messung mit ≥5 wartenden Aufgaben und leeren Plätzen → mehrere geleast.

4) RxDB-Projektion zeigt stornierte Leases weiter als `running`. Nach `ctox queue cancel` blieben Aufgaben in ctox_business_os__ctox_queue_tasks__v1 auf `running`; das CTOX-Modul im Browser zeigt Arbeit, die es nicht gibt. Erwartet: Statusänderungen durch Cancel/Verfall (Punkt 1) werden projiziert. Abnahme: Test Cancel → Projektion `cancelled`.

5) Harness ↔ Business-OS-Writeback. Vier Recherchen waren inhaltlich fertig und gingen beim Zurückschreiben verloren: der Harness-Worker wählte die CLI ("Writeback-Befehl via CLI (Sandbox blockiert SQLite – writeback nicht möglich)"), obwohl der Weg über den Business-Command `outbound.lead.research_writeback` offen war. App-seitig ist der Vertrag seit 1.0.99 explizit (mechanism business_command, forbidden_mechanisms cli/shell/sqlite). Harness-seitig fehlt: (a) der Writeback-Weg muss dem Worker als Werkzeug/Fähigkeit sichtbar sein, (b) ein Writeback-Versuch über gesperrte Wege muss laut scheitern und die Aufgabe als `failed` mit diesem Grund markieren, statt still als erfolgreich zu enden. Dazu: fünf Browser-Kollektionen (user_thread_states, desktop_icons, business_chats, outbound_lead_generation_leads, -adapters) melden dauerhaft initialReplicationState=pending ohne Neustarts, während Daten nachweislich fließen – awaitInitialReplication löst nicht auf. Kleinster Befund zuerst: woran hängt es (Push ohne Ack? Checkpoint?). Nur fixen, wenn Ursache belegt.

Regeln: AGENTS.md gilt (keine neuen Env-Toggles, Guard-Tests nicht schwächen, docs/ctox-rxdb.md vor RxDB-Änderungen lesen). Commits in grünen Scheiben, je Befund mindestens ein Commit, Betreff mit Befundnummer. Keine Subagenten. Validierung: cargo fmt --check, cargo check, gezielte cargo test; für Punkt 4/5 zusätzlich node src/apps/business-os/rxdb/tests/run-all.mjs und cargo test --manifest-path src/core/rxdb/Cargo.toml. Nichts auf der Kundeninstanz ändern.

Berichte nach jedem abgeschlossenen Befund kurz: Ursache (mit Datei:Zeile), Änderung, Testbeleg, Commit-Hash im sauberen Klon, und ob origin/main bereit ist. Wenn du bei einem Punkt die Ursache nicht belegen kannst, sag das und geh weiter – nicht raten.
