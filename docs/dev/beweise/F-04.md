# F-04 — Sellify: 238.913 CRM-Zeilen liegen unerreichbar in v1, die Registrierung nimmt v0

Du bist ein Leaf-Worker. Keine Subagenten. Antworte auf Deutsch.

## Der Live-Befund (vom Orchestrator selbst gemessen, nicht zu wiederholen)

Store `~/.local/state/ctox/business-os-rxdb.sqlite3`:

    ctox_business_os__sellify_companies__v1  = 17.516     __v0 = 0
    ctox_business_os__sellify_campaigns__v1  = 86.549     __v0 = 0
    ctox_business_os__sellify_people__v1     = 60.639     __v0 = 0
    ctox_business_os__sellify_activities__v1 = 74.209     __v0 = 0

Beide installierten Manifeste
(`~/.local/state/ctox/sellify-private-v0428-source/sellify/collections.schema.json`
und `~/.local/state/ctox/thesen-private/sellify/collections.schema.json`)
deklarieren diese vier Collections mit **version = 0**. `sellify_records`
deklariert 1 und hat eine leere v1-Tabelle.

Folge fuer den Nutzer: das CRM ist im Browser leer, obwohl 238.913 Zeilen
im Store liegen.

## Deine Aufgabe — NUR Messung und Ursachenbestimmung, KEIN Fix

Beantworte mit datei:zeile-Belegen und Zahlen aus dem laufenden System:

1. **Wer hat die v1-Zeilen geschrieben?** Es gibt zwei Namenspfade:
   `rxdb_collection_version_table_name` (deklarierte Version,
   `src/core/business_os/rxdb_peer.rs:14708`) und `latest_rxdb_collection_table`
   (neueste vorhandene Tabelle, `:10119`). Zeige, welcher Pfad den
   Sellify-Import/Projektor bedient und welcher die Peer-Registrierung.
   Wenn es ein dritter Pfad ist (`store.rs:1167`, `:1854`, `:15252`), benenne ihn.
2. **Wann entstanden die v0-Tabellen und wann die v1?** Nutze
   `sqlite_master.sql`, Zeitstempel in den Zeilen (`_meta`/`updated_at`/`mtime`)
   und den Log unter `~/.local/state/ctox/logs/` (oder wo der Daemon schreibt),
   um die Reihenfolge zu belegen. War Sellify je auf v1 deklariert und wurde
   heruntergestuft, oder hat ein Writer die deklarierte Version nie beachtet?
3. **Was registriert der laufende Peer heute?** Beleg aus dem Log oder dem
   Peer-Status, welche Tabelle fuer `sellify_people` tatsaechlich angebunden ist.
4. **Greift der SYNC-E-Migrations-Lebenszyklus hier?** In
   `src/core/rxdb/` und `rxdb_peer.rs` existiert seit dem 05.08. ein
   Migrations-/Versions-Invalidierungsvertrag. Sage mit datei:zeile, ob dieser
   Pfad einen v0→v1-Sprung abdecken WUERDE und warum er hier nicht gefeuert hat.
5. **Reparaturoptionen** — nenne genau zwei bis drei, je mit Risiko:
   (a) Manifest auf 1 heben, (b) Daten v1→v0 migrieren, (c) Registrierung auf
   "latest" umstellen. Sage, welche du empfiehlst und warum. Entscheide NICHT
   selbst durch Aenderung von Code oder Daten.

## Harte Grenzen

- **Du aenderst KEINE Datei.** Keine Edits, keine Writes ausser deinem Report.
  Keine `sqlite3`-Schreibbefehle. Nur SELECT, nur lesen.
- Du fasst den Live-Store nicht schreibend an. Er enthaelt Echtdaten des Nutzers.
- Wenn du glaubst, mehr Scope zu brauchen: STOPP und begruende es im Report.

## Report

Schreibe nach `/private/tmp/f-04-report.md`, Abschnitte exakt so benannt:
`## schreibpfad` · `## zeitliche_reihenfolge` · `## peer_registrierung_heute`
· `## migrationsvertrag` · `## reparaturoptionen` · `## unsicherheiten`

Jede Behauptung mit datei:zeile oder einer Zahl. Wo du etwas nicht messen
konntest, schreibe das ausdruecklich hin statt zu vermuten.
