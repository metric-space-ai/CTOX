# R-01 — exakte HEAD-Messung (v2)

## Ergebnisstatus

- Status: **partial** — alle 59 Namen bewertet, aber 0 Testfunktionen erreicht.
- Mess-HEAD: `63700d7d62d6de9eb4e034548fad6de7cde577f0`.
- Verifizierter isolierter Scratch: `/Volumes/tmp/ctox-pipeline/r01-codex-scratch` (kritische Dateihashes identisch zu `git show 63700d7d6:<datei>`).
- Cargo-Target: `/Volumes/tmp/ctox-pipeline/r01-target`; `CARGO_INCREMENTAL=0`; strikt offline.
- Zählung: `echte_regression=0`, `test_veraltet=0`, `umgebung=0`, `unklar=59`.
- Reine Testlaufzeit: 0 s; exakter HEAD-Buildversuch: 386.67 s.

## Checkout-Invariante

- Beginn: 136 Porcelain-Einträge, SHA-256 `155b44dd8de0c09dee76932ed5912cedad1659fe0aca56f4b240bf63319c0182`.
- Nach Messung: 136 Porcelain-Einträge, gleicher SHA-256. Der Checkout wurde nicht beschrieben.

## Laufprotokoll

| Gruppe | Plattenstand / Load vor Lauf | Ergebnis |
|---|---|---|
| `business_os::mcp_channel` (13) | `/dev/disk3s5 926Gi 893Gi 10Gi 99%`; Load `7.42 9.10 8.51` | Exit 101; 52 Compilerfehler; 0 Tests; `r01-exact-head-mcp.log`; real 386.67 s. |

Alle 59 Filter bauen denselben Binär-Testtarget `ctox`. Der exakte, hashverifizierte HEAD erreicht das Test-Harness nicht. Weitere Filter können deshalb keine andere HEAD-Aussage liefern; frühere Gruppenaufrufe im gemeinsam belegten Scratch bestätigten denselben Vor-Harness-Abbruch, wurden aber wegen nachgewiesener Fremdüberschreibung des Scratch nicht als Primärbeleg gewertet.

## Gemeinsamer HEAD-Build-Befund

1. `src/core/execution/cliproxyapi_host.rs:25` importiert `AnthropicHttpTransport`; der Export ist im Pfad-Crate feature-gesteuert, während `Cargo.toml:28` nur `codex-http-transport` aktiviert.
2. `src/core/execution/cliproxyapi_host.rs:214` referenziert `crate::execution::cliproxyapi_integration`, das der archivierte HEAD in `src/core/execution/mod.rs` nicht deklariert.
3. Weitere unabhängige Fehler betreffen fehlende `MainResponsesGatewayPhase`/`main_responses_gateway_status`, Secret-Bulk-APIs und Outbound-Tracking-APIs.
4. `git log -S` führt die `cliproxyapi_host`-Referenzen und die fehlende Integration auf `1a0fd9fc3` (`build: der fehlende CLIProxyAPI-Port kommt in die Versionsverwaltung`) zurück. Das ist der belegte gemeinsame Build-Verursacher, nicht der behauptete Verursacher der 59 historischen Assertion-Fehler.

## Einzelklassifikation

| test | status_head | klasse | beleg datei:zeile | seit_commit | notiz |
|---|---|---|---|---|---|
| `business_os::mcp_channel::tests::audited_mcp_policy_denial_records_business_os_policy_decision` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `business_os::mcp_channel::tests::execute_action_blocks_external_effects_even_when_approved` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `business_os::mcp_channel::tests::list_entities_keeps_non_admin_entities_read_only` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `business_os::mcp_channel::tests::list_entities_marks_writable_entities_from_module_collections` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `business_os::mcp_channel::tests::mcp_business_os_policy_allows_collection_read_via_module_grant` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `business_os::mcp_channel::tests::mcp_business_os_policy_allows_explicit_module_data_write_grant` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `business_os::mcp_channel::tests::mcp_business_os_policy_allows_unpersisted_service_actor_grant_and_audits_identity` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `business_os::mcp_channel::tests::mcp_business_os_policy_filters_module_list_by_app_visibility_not_data_read` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `business_os::mcp_channel::tests::mcp_policy_enforces_actor_workspace_module_and_collection_scopes` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `business_os::mcp_channel::tests::modify_app_tool_enqueues_app_modify_skill_task` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `business_os::mcp_channel::tests::propose_action_returns_non_executing_business_os_command_shape` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `business_os::mcp_channel::tests::support_agent_action_proposal_keeps_typed_payload_unwrapped` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `business_os::mcp_channel::tests::support_module_actions_expose_agent_writeback_contract` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `business_os::rxdb_peer::tests::native_peer_consumes_pending_business_command_written_directly_to_sqlite` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `business_os::rxdb_peer::tests::sync_business_record_projections_materializes_procedural_knowledge` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `business_os::store::tests::app_validation_success_accepts_postlease_artifact_write` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `business_os::store::tests::app_validation_success_preserves_runtime_module_id_with_underscores` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `business_os::store::tests::business_os_why_command_explains_team_visible_one_x_apps` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `business_os::store::tests::configured_auth_user_is_trusted_for_rxdb_admin_commands` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `business_os::store::tests::data_write_chokepoint_gates_fallthrough_data_commands` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `business_os::store::tests::documents_context_chat_appends_pruefnotiz_without_queue` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `business_os::store::tests::exhausted_intake_failure_creates_durable_canonical_evidence` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `business_os::store::tests::replicated_peer_without_token_cannot_manage_secrets` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `business_os::store::tests::source_core_manifests_match_canonical_system_app_manifest` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `context::context_stress::tests::stress_harness_survives_adversarial_summarizer` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `context::lcm::tests::continuity_prompt_contains_document_and_diff_rules` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `execution::models::local_model::tests::qwen36_backend_assembles_expected_cli` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `execution::models::runtime_contract::tests::legacy_gpu_lease_ledger_migrates_into_runtime_ownership_view` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `execution::models::runtime_control::tests::cutover_ready_local_switch_commits_immediately_when_backend_is_ready` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `execution::models::runtime_control::tests::local_runtime_selection_does_not_reuse_stale_persisted_plan_for_new_model` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `execution::models::runtime_control::tests::reconciliation_marks_local_switch_as_warming_when_backend_is_starting` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `execution::models::runtime_control::tests::rollback_runtime_switch_restores_previous_runtime_selection` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `execution::models::runtime_control::tests::warming_local_switch_fails_when_starting_workload_loses_process` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `execution::models::supervisor::tests::managed_chat_launch_env_uses_plan_and_filters_unrelated_flags` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `execution::models::supervisor::tests::managed_runtime_ports_cover_core_backend_defaults_without_boundary_proxy` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `execution::responses::gateway::tests::local_gateway_config_ignores_stale_upstream_override` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `execution::responses::gateway::tests::local_gateway_config_prefers_runtime_port_override` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `knowledge::data::tests::imported_data_evidence_requires_untampered_server_artifact` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `mission::tickets::tests::internal_work_terminology_firewall_keeps_self_work_legacy_only` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `mission::tickets::tests::source_skill_review_note_accepts_plain_grounded_internal_note` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `mission::tickets::tests::source_skill_review_note_flags_leaky_or_copied_notes` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `mission::tickets::tests::source_skill_show_and_query_use_bound_operating_model_artifact` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `mission::tickets::tests::ticket_close_is_blocked_without_verified_guard_proof` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `module_boundary_tests::tests::module_boundaries_hold_within_their_declared_budget` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `report::tests::asset_pack_roundtrip::asset_pack_has_seven_report_types` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `report::tests::cli_smoke::figure_add_image_file_persists_row_and_copies_image` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `report::tests::rascon_replay::rascon_replay_produces_feasibility_shaped_manuscript` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `service::business_os::tests::app_bench_run_submits_real_tasks_without_writing_app_artifacts` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `service::business_os::tests::app_bench_status_records_partial_artifacts_without_marking_green` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `service::business_os::tests::app_create_cli_enqueues_real_task_without_writing_app_artifacts` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `service::db_migration::tests::merges_legacy_files_into_ctox_db` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `service::service_loop::tests::approval_nag_idle_gate_reopens_when_core_db_changes` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `service::service_loop::tests::business_os_app_queue_jobs_use_lean_mcp_free_session` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `service::service_loop::tests::business_os_app_recovery_idle_gate_ignores_unrelated_churn_and_reopens_on_app_sources` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `service::service_loop::tests::reviewed_founder_reply_closes_stale_rework_item` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `service::service_loop::tests::stalled_founder_email_requeues_blocked_rework` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `service::service_loop::tests::stop_guard_blocks_leased_business_os_rxdb_app_queue_task` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `service::state_write_guard::tests::protected_state_writes_are_core_guarded_or_explicit_test_fixtures` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |
| `tests::appsec_worker_dispatches_business_os_web_stack_auth_assist_contract` | nicht_ausgeführt: gemeinsamer `ctox`-Testtarget kompiliert auf HEAD nicht | unklar | `src/core/execution/cliproxyapi_host.rs:25`, `src/core/execution/cliproxyapi_host.rs:214`, `Cargo.toml:28` | unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3` | Keine Assertion erreicht, daher keine aktuellen links/rechts-Werte und keine zulässige Individualklassifikation. |

## Fünf größte Gruppen — Vertreter

### mcp_channel (13)

`list_entities_keeps_non_admin_entities_read_only` legt ein Modul `customers` an und fordert genau eine Entity `customer_accounts` mit `read_only == true` (`src/core/business_os/mcp_channel.rs:7298-7313`). Aktueller HEAD-Wert: keiner, weil der Compiler vor dem Harness stoppt. Historischer, nicht als HEAD-Nachmessung gewerteter Befund: `ModuleNotFound: ... customers ...` (`/Volumes/tmp/ctox-pipeline/final.log:130-131`). Test seit `0c50394c9`; individueller Verursacher unklar; gemeinsamer Build-Blocker seit `1a0fd9fc3`.

### store (9)

`source_core_manifests_match_canonical_system_app_manifest` fordert 10 kanonische Core-IDs, Mengengleichheit und 24 Marketplace-Manifeste (`src/core/business_os/store.rs:32696-32729`). Aktueller HEAD-Wert: keiner. Historisch: `left: 12`, `right: 10` (`final.log:246-251`). Test seit `7f10199e7`; absichtlicher Katalogwechsel versus Regression ohne HEAD-Lauf unklar; Build-Blocker seit `1a0fd9fc3`.

### service_loop (6)

`approval_nag_idle_gate_reopens_when_core_db_changes` fordert für `should_skip` die Folge `false`, dann `true`, nach Business-OS-Churn weiter `true`, nach Core-DB-Änderung wieder `false` (`src/core/service/service.rs:28704-28744`). Aktueller HEAD-Wert: keiner. Historisch scheiterte die letzte Erwartung (`final.log:438-441`). Test seit `38c347e6c`; individueller Verursacher unklar; Build-Blocker seit `1a0fd9fc3`.

### tickets (5)

`ticket_close_is_blocked_without_verified_guard_proof` erwartet vor Verifikation einen Fehler mit `closure_requires_verification`, danach `writeback_pending`, `closed` und je einen akzeptierten/abgelehnten Proof (`src/core/mission/tickets/tests.rs:3603-3685`). Aktueller HEAD-Wert: keiner. Historisch fehlte der Marker im Fehlertext (`final.log:393-396`). Testprovenienz mindestens bis `1a777e826`, Verschiebung nach `tests.rs` in `bc7f1e574`; individueller Verursacher unklar; Build-Blocker seit `1a0fd9fc3`.

### runtime_control (5)

`cutover_ready_local_switch_commits_immediately_when_backend_is_ready` erwartet `Some(Committed)` vom Commit-Aufruf und `Committed` in der persistierten Transaktion (`src/core/execution/models/runtime_control.rs:2931-3007`). Aktueller HEAD-Wert: keiner. Historisch: `left: None`, `right: Some(Committed)` (`final.log:309-314`). Testprovenienz mindestens bis `ca9883e44`/`1a777e826`, Layout-Verschiebung in `191a47ad6`; individueller Verursacher unklar; Build-Blocker seit `1a0fd9fc3`.

## Auffälligkeiten

- Der Nachtrag v2 beseitigte die Offline-Abhängigkeitsauflösung, nicht den belegten HEAD-Quellbruch.
- Der gemeinsame Scratch `/Volumes/tmp/ctox-pipeline/r01-scratch` wurde während der Messung durch einen fremden Prozess ersetzt/überschrieben. Deshalb wurde der Primärbeleg in einem separaten, hashverifizierten Scratch unter derselben erlaubten Volume-Hierarchie wiederholt.
- Die 59 historischen roten Assertionen können auf HEAD weder bestätigt noch widerlegt werden. `unklar` ist zwingend; eine Klassifikation als `umgebung` wäre falsch, weil der aktuelle Blocker Produktions-/Buildcode betrifft und die individuellen Tests gar nicht liefen.
