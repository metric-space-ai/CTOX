# S-02b — Die Sync-Engine verliert ihr Browser-Control-Subsystem (move-only)

## Warum

`src/core/business_os/rxdb_peer.rs` ist das God-Modul der Sync-Engine:
14.744 Produktionszeilen, im Groessenbudget seit Commit 27ca6b205 festgeschrieben
(`contracts/module_size_budget.txt`). Der Browser-Control-Teil ist ein
abgeschlossener Belang: 28 `browser_*`-Funktionen, Einstieg
`apply_browser_runtime_command` (:5510), Wartungsschleife
`browser_runtime_maintenance_loop` (:6246).

Ein frueherer Anlauf hatte 2.821 Zeilen herausgehoben, ging aber beim Abbruch
verloren. Die extrahierte Datei liegt noch unter
`/Volumes/tmp/ctox-s02/src/core/business_os/rxdb_peer_browser.rs` — du darfst
sie als Vorlage LESEN, musst den Schnitt aber selbst und vollstaendig machen.

## Auftrag: reiner Umzug

Neues Modul `src/core/business_os/rxdb_peer_browser.rs`, angemeldet in
`src/core/business_os/mod.rs`.

REINER UMZUG heisst: kein Umbenennen, keine Signaturaenderung, keine
Verhaltensaenderung, keine Verbesserung im Vorbeigehen. Nur verschieben plus die
noetigen `use`-Zeilen und Sichtbarkeiten. Ein Diff, der etwas anderes tut als
verschieben, ist ein Fehlschlag dieses Auftrags. Der Grund: dieser Baum wird
gegen fremde, unversionierte Arbeit zusammengefuehrt — nur ein reiner Umzug
laesst sich verlaesslich mischen.

Danach `contracts/module_size_budget.txt`: die Zahl fuer `rxdb_peer.rs` auf den
neuen, niedrigeren Istwert senken. Zaehlweise steht im Kopf der Kontraktdatei.

Der unerreichbare zweite Browser-Befehlsblock bei :5987-5994 (aus dem
Qualitaetsreview vom 27.07.) wird MITVERSCHOBEN, nicht geloescht. Sein Zustand
gehoert in den Report, nicht in diesen Diff.

## Harte Grenzen

- Aendern darfst du NUR: `src/core/business_os/rxdb_peer.rs`,
  `src/core/business_os/rxdb_peer_browser.rs` (neu),
  `src/core/business_os/mod.rs`, `contracts/module_size_budget.txt`.
- Alles andere ist verboten, insbesondere `store.rs` und `server.rs`.
- Brauchst du mehr Scope: STOPP und begruende es im Report.
- Keine Subagenten.

## Abnahme

    cargo fmt --check
    cargo check --bin ctox --tests
    cargo test --bin ctox -j 4 -- --test-threads=1 module_size_tests::
    cargo test --bin ctox -j 4 -- --test-threads=1 business_os::rxdb_peer::tests

`--tests` ist Pflicht: `cargo check --bin ctox` allein uebersetzt die Tests
nicht, und Signatur- oder Sichtbarkeitsfehler fallen dann erst spaeter auf.

WICHTIG: `origin/main` ist mit 59 seriell reproduzierbaren Fehlschlaegen rot.
Tor ist "keine NEUEN Fehlschlaege gegenueber deiner Basis". Miss die Basis
zuerst und lege beide Zahlen in den Report.

Commit-Nachricht (nur wenn gruen):
`business_os: der Peer gibt seine Browsersteuerung ab (reiner Umzug)`

## Report-Ende (feste Form)

    GEAENDERT: <pfade>
    VERSCHOBENE FUNKTIONEN: <anzahl>
    RXDB_PEER_ZEILEN: vorher <n> nachher <n>
    BASIS-ROT: <zahl>   MEINE-ROT: <zahl>   NEU-ROT: <namen oder keine>
    UNERREICHBARER BLOCK :5987: <was du vorgefunden hast>
    OFFENE BEDENKEN: <text oder keine>
