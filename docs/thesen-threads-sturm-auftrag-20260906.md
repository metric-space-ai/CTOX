Neuer Auftrag (Business-OS-Datenebene, gleicher Rahmen wie gestern): Das Threads-Modul erzeugt im Browser einen Bedarfsabfrage-Sturm, der die Zustellung von Business-Befehlen um 15–20 Minuten verzögert; die Outbound-App bricht deshalb jeden Recherchestart ab. Vollständiger Feldbefund mit Messwerten: docs/ctox-sync-feldbefund-20260906-erstpull.md (auf origin/main, Stand nach 21:00 UTC). Lies ihn zuerst, inklusive der KORREKTUR-Abschnitte.

Belegte Kette (Kundeninstanz, Release branch-main-20260905T072559Z, App 1.0.100):
1. modules/threads/index.js: refresh() (Zeile ~442) lädt je Lauf ctox_task_approval_requests ZWEIMAL (recentQuery(200,{status:'pending'}) und recentQuery(200)) plus user_thread_states; refresh() wird von einem setInterval (Zeile ~416), von jedem Bereitschaftswechsel (wireReadiness → render) und ~20 weiteren Stellen ausgelöst. Auf nicht-`live`-Kollektionen ist jede find() eine Bedarfsabfrage über den Draht → Konsole „[V1.5] fetch:start ctox_task_approval_requests" im Sekundentakt, 3793 Meldungen, 0 Abschlüsse.
2. Die Frames füllen die Sendewarteschlange des Browsers (business_commands.frameTransport: queued 3098→3491 in 8 min); Befehls-Frames stehen dahinter an. Gemessen: Klick 18:41 → outbound.sellify.lookup completed 18:58; Klick 19:19 → 19:40.
3. Die App wartet auf den Sellify-Vorabgleich 120 s (researchLead, withTimeout 120000) und bricht ab: „Der Sellify-Abgleich ist fehlgeschlagen … bitte erneut versuchen". Keine Recherche wird eingereiht. Screenshot des Eigentümers liegt vor.
4. Gegenbeweis: Seite laden und VOR dem Modulstart app.sync.suspendCollections(['ctox_task_approval_requests','user_thread_states']) → Erst-Pull von outbound_lead_generation_leads erstmals `live`, Recherchestart 20:45:16 → Lookup completed 20:45:38 → chat.task accepted 20:45:43 → geleast 20:45:49. 27 s statt 20 min.
5. Nebenbefund: user_thread_states (3911 Docs) bleibt dauerhaft `catching-up`/`initialReplicationState pending` und liefert die Bereitschaftswechsel, die refresh() befeuern — Rückkopplung.

Auftrag, in Priorität:
A) Threads-Modul: kein Abfragesturm mehr. refresh() darf nicht bei jedem Bereitschaftswechsel und nicht per Intervall Volltext-Abfragen über den Draht auslösen; Bedarfsabfragen auf nicht-`live`-Kollektionen entkoppeln/entdoppeln (eine offene Abfrage je Fingerprint, Wiederholung nur bei Datenänderung oder mit Backoff). Abnahme: Test, der refresh() n-mal auslöst und höchstens eine offene Bedarfsabfrage je Fingerprint zählt; Browser-Smoke in src/apps/business-os/rxdb/tests, der fetch:start-Rate für ctox_task_approval_requests deckelt.
B) Bedarfsladung darf Befehlszustellung nicht verdrängen: business_commands-Push (und Push generell) muss Vorrang vor Demand-Fetch-Frames haben oder eine eigene Bahn bekommen (frameTransport hat priorityQueueDepth/highPriorityQueueDepth — prüfen, ob Befehle dort landen; sie taten es offenbar nicht). Abnahme: Test mit 500 gequeueten Demand-Frames, ein Push-Frame wird trotzdem innerhalb eines Ack-Fensters gesendet.
C) Prüfe zuerst, ob die seit 05.09. auf origin/main gelandeten Sync-Commits (z. B. „fix(sync): stop retired background transfers at awaited boundaries", „preserve direct bridges and isolate DB runtime coordinators", „retire replaced collection registrations safely") A/B bereits abdecken — wenn ja, benennen und nur den Rest bauen; wenn nein, sagen, was sie nicht abdecken.
D) Nachrangig: warum user_thread_states nie `live` wird (3911 Docs, 2,3 MB, kein Draht-Budget-Verstoß); nur fixen, wenn belegt.
E) App-seitig (Kunden-App, nicht im Repo — das mache ich): keine Änderung nötig, wenn A/B greifen; sag mir, ob ein längerer Vorabgleich-Timeout sinnvoll bleibt.

Regeln wie gestern: Arbeit in diesem Checkout (abgedriftet), Landen ausschließlich über einen sauberen Klon von origin/main mit cargo check + JS-Suite (node src/apps/business-os/rxdb/tests/run-all.mjs, Identifier-Guard muss grün bleiben), Commits je Punkt, keine Kundeninstanz anfassen, kein Env-Toggle, Guards nicht schwächen, keine Subagenten. Bericht je Punkt: Ursache Datei:Zeile, Änderung, Testbeleg, Commit im Klon, ob origin/main bereit ist. Das Tenant-Upgrade führe ich aus.
